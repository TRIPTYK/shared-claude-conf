# Claude Code Plugin Skeleton

This folder contains a minimal starter layout for a Claude Code plugin.

## Structure

- `plugin.json` - basic plugin metadata and entrypoint.
- `package.json` - minimal Node.js package and `start` script.
- `src/index.js` - starter activation function and CLI entrypoint.

## Run

```bash
npm install
npm start
```
